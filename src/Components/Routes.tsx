const Home = "/";

const Pass = "/pass/:id";

const SearchMain = "/search";
const SearchResult = "/search/result";

const Form = "/form/:placeid";

export default {
  Home,
  Pass,
  SearchMain,
  SearchResult,
  Form,
};
