import React from "react";
import UserEditProfilePresenter from "./UserEditProfilePresenter";

const UserPassContainer: React.FunctionComponent = () => {
  return <UserEditProfilePresenter />;
};

export default UserPassContainer;
